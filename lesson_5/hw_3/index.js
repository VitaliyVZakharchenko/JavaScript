'use strisct'






const sum = function (from, to) { /*сума чисел*/
    let sum = 0;
    for (let i = 1; i <= n; i++) {
        sum = sum += i;
    }
}


const compareSums = function (a, b, c, d) {
    if (sum1(a, b) > sum2(c, d)) {
        return 
    }
}